import { Res } from "../../../types/response";

export const Card = ({
  height,
  pokeName,
  sprites,
  stats,
  types,
  weight,
}: Res) => {
  return <div className="border-2 border-black rounded-xl"></div>;
};
